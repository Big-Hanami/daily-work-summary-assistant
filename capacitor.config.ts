import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.dailywork.summaryassistant",
  appName: "Daily Work Summary Assistant",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
